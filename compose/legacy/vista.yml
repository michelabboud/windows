include:
  - ../base-legacy.yml

services:
  vista:
    <<: *legacy
    container_name: vista
    env_file: ../../.env
    environment:
      VERSION: "vu"
    ports:
      - 8006:8006
      - 3306:3389/tcp
      - 3306:3389/udp
    volumes:
      - ../../data/vista:/storage
