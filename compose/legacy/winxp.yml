include:
  - ../base-legacy.yml

services:
  winxp:
    <<: *legacy
    container_name: winxp
    env_file: ../../.env
    environment:
      VERSION: "xp"
    ports:
      - 8005:8006
      - 3305:3389/tcp
      - 3305:3389/udp
    volumes:
      - ../../data/winxp:/storage
