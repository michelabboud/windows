include:
  - ../base-legacy.yml

services:
  win2k:
    <<: *legacy
    container_name: win2k
    env_file: ../../.env
    environment:
      VERSION: "2k"
    ports:
      - 8000:8006
      - 3300:3389/tcp
      - 3300:3389/udp
    volumes:
      - ../../data/win2k:/storage
